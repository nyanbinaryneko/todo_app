<%= simple_form_for @task, url: [@list, @task] do |f| %>
    <%= f.input :description %>
    <%= f.input :status, include_blank: false %>
    <%= f.button :submit, 'Edit a Task', id: 'task_submit' %>
<% end %>