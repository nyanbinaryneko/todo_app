<div class="jumbotron">
    <h1>Lists</h1>
</div>
<div>
    <ul>
        <% @lists.each do |list| %>
            <li><%= link_to list.name, list %></li>
        <% end %>
        <%= link_to "Add a new list" , [:new, :list]%>
    </ul>
</div>
